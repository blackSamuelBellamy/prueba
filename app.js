const container = document.querySelector('.container');

/* setInterval(() => {
    let vh = (window.innerHeight * .01) + 'px';
    container.style.setProperty('--var', vh);
    console.log(vh);
}, 200) */
 